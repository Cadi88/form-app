import { NoNumberDirective as noNumbersDirective } from './no-number.directive';

describe('MyDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new noNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
